"""Observer advantage through unrelated codecs (docs/PREREG_observer_advantage.md).

Does the consumer-read basis O, which beats corpus PCA under exact search on
asymmetric retrieval (benchmarks/RESULTS_consumer_basis.md), still beat it once
the kept subspace is compressed by TurboQuant, RaBitQ and OPQ at matched bytes?
Arms, staged arrays and bases come from ``consumer_basis``; the cell statistic
is ``consumer_basis.score``'s, imported, not copied.
"""
