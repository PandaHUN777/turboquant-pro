"""Public-data comparison of turboquant-pro against RaBitQ, PQ and OPQ at matched stored bytes.

Preregistration: docs/PREREG_rabitq_public.md. One cell = one (dataset, method, budget, seed)
run writing one JSON; ``grid.py`` is the frozen cell list, ``score.py`` applies the
registered verdict rules. Run on NRP through ``submit_pool.py``.
"""
